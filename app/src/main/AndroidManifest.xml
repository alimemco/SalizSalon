<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.alirnp.salizsalon">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <dist:module dist:instant="true" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_female"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_female_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".HolderForTestApp.ActivityShop"></activity>
        <activity android:name=".Views.Activities.ActivityRegister"></activity>
        <activity android:name=".Views.Activities.ActivityLogin" />
        <activity android:name=".Views.Activities.ActivityChooseTime" />
        <activity android:name=".Views.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>